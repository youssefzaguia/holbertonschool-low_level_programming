#include "main.h"

/**
 * _islower -  prints _putchar, followed by a new line
 * Return: Always 0 (Success)
 * @c : integer
 */

int _islower(int c)
{

	if (c >= 97)
	{
		return (1);
	}
	else
		return (0);
}
